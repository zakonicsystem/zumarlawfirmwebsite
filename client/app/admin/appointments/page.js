import AdminClient from "@/components/admin/AdminClient";

export const metadata = {
  title: "Appointments"
};

export default function AdminAppointmentsPage() {
  return <AdminClient view="appointments" />;
}
